import Sidebar from "../Sidebar"

const Layout = () => {
    return (
        <div className="bg-gradient-to-r from-black via-gray-900 to-black min-h-screen flex max-w-full mx-auto">
            <Sidebar />
        </div>
    )
}

export default Layout